# Copy to .env and adjust. Loaded by Node's built-in --env-file-if-exists.
PORT=3000

# Signs the session cookie. Any long random string; change it for anything public.
SESSION_SECRET=replace-me-with-a-long-random-string

# SQLite file, resolved relative to the server/ directory.
DB_PATH=./gymrank.db

NODE_ENV=development
